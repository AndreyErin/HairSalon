<section>
    <div class="container text-center">
        <div class="row">
            <h1>Сраница администратора</h1>
        </div> 


        <div class="row">
            <div class="col text-end">
                @User.Identity?.Name
            </div>
            <div class="col text-start">
                <form asp-action="LogOut" asp-controller="Account" method="post">
                    <button class="btn btn-sm btn-warning">Выход</button>
                </form>
            </div>
        </div>


    </div>
</section>

<section class="bg-success-subtle">
    @await Component.InvokeAsync("Records")
</section>

<section>
    @await Component.InvokeAsync("Services")
</section>

<section class="bg-success-subtle">
    @await Component.InvokeAsync("Configuration")
</section>

<section>
    @await Component.InvokeAsync("Employees")
</section>